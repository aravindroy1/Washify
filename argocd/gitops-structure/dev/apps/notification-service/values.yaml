# Auto-updated by dev team after ci-dev pipeline runs
# Image pushed by: ci-dev.yml (commit sha tag)
replicaCount: 1
image:
  repository: ghcr.io/washifyapp/notification-service
  tag: sha-latest   # <-- update this after dev pipeline runs
  pullPolicy: Always
namespace: washify-dev-ns
