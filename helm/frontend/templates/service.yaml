apiVersion: v1
kind: Service
metadata:
  namespace: washify-dev-ns
  name: frontend-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: frontend-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}

