apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  namespace: washify-dev-ns
  name: washify-gateway
  namespace: default
spec:
  gatewayClassName: kgateway
  listeners:
  - name: http
    protocol: HTTP
    port: 80
